name: remove_background
description: Remove background from the image
version: 0.0.1
homepage: https://github.com/DakshDB/remove_background

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  image_pixels: ^2.0.1
  image: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter:
